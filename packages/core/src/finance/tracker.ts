export class BudgetTracker {
  async track(projectId: string): Promise<any> { return {}; }
  async forecast(projectId: string): Promise<any> { return {}; }
  async alert(projectId: string): Promise<any[]> { return []; }
}