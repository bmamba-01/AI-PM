export class QAWorkflow {
  async createTestPlan(storyId: string): Promise<any> { return {}; }
  async runExploratoryTesting(sessionId: string): Promise<any> { return {}; }
  async automateTestCase(testId: string): Promise<void> {}
  async triageBug(bugId: string): Promise<any> { return {}; }
}